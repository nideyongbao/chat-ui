# ----------------------------------------------------------
# ✅ 基础模型来源
# ----------------------------------------------------------
from gemma3:1b

# ----------------------------------------------------------
# ✅ 系统提示词 (system prompt)
# ----------------------------------------------------------
system """
你是一名专业的AI助手，擅长解释技术问题并提供结构化答案。
在回答时请尽量包含示例、代码块和必要的解释说明。
"""

# ----------------------------------------------------------
# ✅ 模型参数 (推理参数)
# ----------------------------------------------------------
parameter temperature 0.7
parameter top_p 0.9
parameter top_k 40
parameter num_ctx 4096
parameter num_predict 512
parameter repeat_penalty 1.1
parameter seed 42
parameter mirostat 0
parameter stop "User:"
parameter stop "</s>"
parameter stop "<|im_end|>"

# ----------------------------------------------------------
# ✅ 模板 (prompt 模板)
# ----------------------------------------------------------
template """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Response }}<|im_end|>"""

# ----------------------------------------------------------
# ✅ （可选）加载 LoRA 适配器（单数关键字 adapter）
# ----------------------------------------------------------
# adapter ./adapters/finance-lora

# ----------------------------------------------------------
# ✅ License 信息
# ----------------------------------------------------------
license "Apache-2.0"

# ----------------------------------------------------------
# ✅ 示例 Message (在创建模型时附带一个样例对话)
# ----------------------------------------------------------
message system "你是一个友善的助手。"
message user "你好！"
message assistant "你好，我能帮你做什么？"
